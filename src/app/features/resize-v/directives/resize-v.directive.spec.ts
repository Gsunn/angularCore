import { ResizeVDirective } from './resize-v.directive';

describe('ResizeVDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeVDirective();
    expect(directive).toBeTruthy();
  });
});
